<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.itrip.dao.hotelroom.HotelroomMapper">
    <select id="getItripRoomBedList" resultType="cn.itrip.beans.pojo.ItripHotelRoom">
         select
                id as id,
                hotelId as hotelId,
                roomTitle as  roomTitle,
                roomPrice as roomPrice,
                roomBedTypeId as roomBedTypeId,
                isHavingBreakfast as isHavingBreakfast,
                payType as payType,
                satisfaction as satisfaction,
                isBook as isBook,
                isCancel as isCanncel,
                isTimelyResponse as isTimelyResponse,
                creationDate as isTimelyResponse,
                createdBy as createdBy,
                modifyDate as modifyDate,
                modifiedBy as modifiedBy
                FROM  itrip_hotel_room
    </select>

</mapper>